# KnitTools v3 Implementation Note — Pattern Viewer Overflow Menu

**Context:** v3-delta-spec defines Pattern Viewer TopAppBar overflow menu as: annotation mode, page jump, detach pattern.

**Change:** Add "Change pattern" action to the overflow menu alongside "Remove pattern" (rename "detach pattern" to "Remove pattern" for clarity).

Final overflow menu order:
1. Page jump
2. Annotation mode (Pro)
3. Change pattern
4. Remove pattern
