# KnitTools Memory

- 2026-08-03: Tarkistus- ja riippuvuusturvan nykyraja käyttää advisory-kohtaisia, määräaikaisia OSV `IgnoredVulns` -poikkeuksia poikkeuksettoman raakaskannauksen rinnalla. Aktiivinen Wire Runtime pakotetaan juuribuildissä versioon 6.3.0, Sonar-ulkoislähetys vaatii `-AllowExternalUpload`-luvan ja hallittu Gradle-prosessi katkaistaan oletuksena tunnissa. Compose Stability Analyzerin `app/stability/app-debug.stability` on versionoitu debug-baseline; muiden varianttien dumpit pysyvät paikallisina.
- 2026-08-03: Insightsin repositoryvirrat kääritään eksplisiittiseen loading/loaded-tilaan ennen `InsightsUiState`-aggregointia, jotta `stateIn`-alkuarvon tyhjä lista ei vaihda latausskeletonia tyhjätilaan ennen Roomin ensimmäistä emissiota. Min/rivi lasketaan `durationSeconds`-arvosta ennen pyöristystä, All Time -akselin ensimmäinen päivä ratkaistaan jokaisen session tallennetussa `zoneId`-vyöhykkeessä, ja suodatinsirujen mitat sekä muoto ovat `InsightsDimens`-tokeneita.
- 2026-07-30: Insights-näytön UI/UX-korjaukset vietiin läpi 17 tehtävän suunnitelmana. Aikaväli irrotettiin hero-lauseesta omaksi kicker-rivikseen (`InsightsRangeKicker` + `insights_range_open_format`), koska taivutuskielissä muotoiltua päivämäärää ei voi interpoloida lauseen sisään; `insights_range_since_format` poistettiin kaikista 11 lokaalista. Kaaviossa asteikon maksimi on plotin yläreunassa (`insights_chart_max_format`), akselileimat harvennetaan `axisLabelIndices`-apurilla, tyhjä päivä on tyhjä sarake jatkuvalla perusviivalla, ja valinta liikkuu napautuksella, vaakavedolla (`detectHorizontalDragGestures` + haptiikka) sekä ruudunlukijan kahdella custom actionilla. Projektirivi avaa laskurin `CounterLaunchRequest`-mekanismilla. Rakenteelliset päätökset on lukittu `InsightsRedesignSourceTest`- ja `InsightsCopySourceTest`-lähdetesteihin.
- 2026-07-28: Insights on typografiavetoinen editoriaalinen näyttö ilman korttisäiliöitä: `InsightsScreen` (runko + suodattimet), `InsightsSections` (kicker, hero, tilastorivi, projektilista, tyhjät tilat), `InsightsChart` (pylväät + lukema) ja `InsightsChartModel` (puhdas ämpäri- ja valintalogiikka). `ActivityGrid`-lämpökartta on poistettu. Kestot kulkevat `domain/calculator/DurationDisplayFormatter`-slottien ja `ui/components/DurationText`-Compose-apurin kautta — desimaalitunteja ei muodosteta missään Insightsissa. Tahtimittari on `MinutesPerRowFormatter` (min/rivi), ei rivit/tunti. `InsightsViewModel` tekee yhden all-time sessiokyselyn per projektisuodatin ja rajaa aikavälin muistissa; erillinen heatmap-kysely poistui. Projektien värit tulevat `ui/theme/yarnColorForId(projectId)`-apurista, ei listan järjestyksestä. Mitat ovat `ui/theme/InsightsDimens`-tokeneissa laskurin `CounterDimens`-mallin mukaisesti.
- 2026-07-28: MobSF:n targetSdk-lähdemanifestin false positive ei ole enää globaali `.mobsf`-rule-ignore. Yhteinen Android-check suodattaa vain `android_task_hijacking2` + `app/src/main/AndroidManifest.xml` -yhdistelmän määräaikaisen `config/check-exceptions.json`-merkinnän perusteella; saman säännön muu polku jää löydökseksi.
- 2026-07-24: Ravelry-backendin shardatun globaalirajan rollout tarkistaa aktiivisen legacy `<bucket>_global` -dokumentin jokaisella pyynnöllä ennen shardien käyttöä, jotta vanha Functions-revisio ei voi luoda saman lämpimän prosessin ohittamaa legacy-ikkunaa. Tarkistus on väliaikainen ja poistetaan erillisessä siivouksessa vasta, kun deploy on valmis ja kaikki legacy single-document -kirjoittajat on vahvistettu poistuneiksi liikenteestä. UID- ja global-dokumentit luetaan yhden `Transaction.getAll()`-kutsun kautta; `rateLimit.ts` säilyttää runtime-tilassa vain kaikkien kymmenen shardin tarkistuksen jälkeen vahvistetun kylläisen fixed-window-ikkunan. Transaktiokohteet ratkaistaan UID/global-scopejen, ei taulukkoindeksien, perusteella.
- 2026-07-22: Ravelry-backendin globaalit auth/search/import-rajat käyttävät `ravelryRateLimits`-kokoelmassa kymmentä tasakapasiteettista kiinteän aikaikkunan shard-dokumenttia yhden yhteisen hotspot-dokumentin sijaan. `functions/src/ravelry/rateLimit.ts` kokeilee täyden shardin jälkeen seuraavaa ja kuluttaa käyttäjäkohtaisen sekä globaalin shard-rajan samassa Firestore-transaktiossa, joten shardien yhteiskapasiteetti säilyttää rajat 60/120/80. OAuth state- ja token-storejen connection-generation-normalisointi on keskitetty `functions/src/ravelry/connectionGeneration.ts`-moduuliin.
- 2026-07-18: Debug-Firebase-resurssien ainoa lähde on Google Services -pluginin generoima output ignored `app/src/debug/google-services.json` -placeholderista tai oikeasta ignored `app/google-services.json` -konfigista. Käsin ylläpidettyä `app/src/debug/res/values/debug_firebase_options.xml` -tiedostoa tai omia `google_app_id`-, `google_api_key`- ja `project_id`-resursseja ei pidä palauttaa, koska ne toisintavat pluginin generoiman konfiguraation.
- 2026-07-18: Debug-esimerkkidatan juuritason `DemoDataSeeder` on vain build-variant facade. `data/local/DebugDemoDataSeeder` orkestroi koko idempotentin seed-operaation yhden injektoidun `DatabaseTransactionRunner`-transaktion sisällä ja käyttää countereille `ProjectCounterRepository`- sekä lanka/projektilinkeille `YarnCardRepository`-kirjoittajia, jotta tuotannon domain- ja kaksisuuntaiset linkityssäännöt säilyvät; release-toteutus pysyy no-opina.
- 2026-07-18: Room schema 17 adds document-keyed `pattern_annotation_layers` and versioned `pattern_annotations`. Saved patterns own reusable master layers; projects own editable overlay layers that are activated on attach and deactivated, not deleted, on detach. Normalized page-coordinate geometry is owned by the domain model, `PatternAnnotationViewModel` commits only at gesture/command boundaries, `PatternAnnotationCanvasRenderer` serves both the viewer and rasterized SAF export, and project chart trackers derive highlights from current main/extra counter values without persisting stale progress.
- 2026-07-14: UI:lle palautuvat Room-havaintovirrat käyttävät repository-rajassa yhteistä `retryOnRepositoryReadFailure`-operaattoria DAO->domain-mäppäyksen jälkeen. Se läpäisee peruutuksen ja yrittää muut luku- tai mäppäyspoikkeukset uudelleen rajatulla eksponentiaalisella viiveellä, jotta yksittäinen virhe ei lopeta `stateIn`-jakokorutiinia tai suoraa ViewModel-keräystä pysyvästi.
- 2026-07-13: DataStoren `hapticFeedback`-asetus portittaa nyt koko Compose-puun `ui/PreferenceAwareHapticFeedback`-providerissa, joten myös komponenttien oletushaptiikka kuten `combinedClickable`-pitkäpainallus hiljenee asetuksen mukana. Counterin eksplisiittinen värinäcallback pysyy oman preferenssitarkistuksensa takana, ja lisälaskurin plus/miinus-polku kutsuu sitä vain action-kerroksessa kerran.
- 2026-07-13: Sovelluksen ViewModelia pidempään elävä viimeinen persistointi käyttää nyt Hiltin `di/DispatchersModule`-moduulin omistamaa `@ApplicationScope`a. `NotesEditorViewModel` ja `CounterViewModel` käynnistävät poistumistallennuksen tämän scopen lapsena injektoidulla `@IoDispatcher`illa; irrallisia `CoroutineScope(ioDispatcher + NonCancellable)` -juuricoroutineja ei käytetä.
- 2026-07-12: Room schema version 16 adds nullable `sessions.zoneId`. New counter sessions persist the device timezone captured at session start, while legacy null values fall back to the current device timezone. `SessionMetrics` uses the persisted session zone for local-day and pace-bucket boundaries and allocates rounded daily seconds cumulatively so cross-midnight segment totals cannot exceed the session duration.
- 2026-07-08: Androidin Ravelry backend response mapperit säilyttävät nyt `PatternAvailability`-kolmitilan (`free`/`paid`/`unknown`) sekä `PatternDetail`in backendin `canonicalUrl`/`originalUrl`-metadatan. `RavelryBackendMappers.patternDetailFrom` hylkää puuttuvan tai ei-positiivisen `ravelryPatternId`-arvon sen sijaan, että se loisi ID 0 -patternin; Ravelry-kortit näyttävät unknown-saatavuuden omana tekstinään eivätkä maksullisena.
- 2026-07-08: Ravelry backendin stored-token käyttö keskitettiin `functions/src/ravelry/tokenAccess.ts`-polkuun. `ravelryCurrentUser`, `ravelrySearchPatterns`, `ravelryImportPatternById` ja `ravelryImportPatternByUrl` virkistävät vanhentuneen `ravelryTokens/{uid}` access tokenin server-side `RAVELRY_CLIENT_ID`/`RAVELRY_CLIENT_SECRET` -secreteillä vasta operaation rate limit -hyväksynnän jälkeen; jos refresh-vastaus sisältää uuden refresh tokenin, se korvaa vanhan tallennetun arvon vain saman `connectionGeneration`-sukupolven ollessa edelleen voimassa. `ravelryDisconnect` kasvattaa generation-tombstonea, jotta myöhäinen callback/current-user/refresh-kirjoitus ei voi palauttaa katkaistua yhteyttä.
- 2026-07-07: Ravelry share-import hardening: Android `ACTION_SEND text/plain` Ravelry URLit avautuvat ensin `RavelryImportStatus.AwaitingUserConfirmation` -tilaan, eikä `RavelryViewModel.showImportConfirmationForUrl` kutsu backendia ennen `confirmImportRequest`-käyttäjäelettä. Firebase Functions Ravelry auth/search/import -callablet kuluttavat `functions/src/ravelry/rateLimit.ts` -toteutuksen per-UID- ja kymmenen shardin backend-global Firestore-bucketteja kokoelmassa `ravelryRateLimits`.
- 2026-07-06: Progress photo capture-target creation and abandoned-capture cleanup moved behind `ProgressPhotoRepository`/`CounterViewModel` and `@IoDispatcher`. `PhotoGalleryScreen` receives `onCreatePhotoCaptureTarget` and `onDeletePendingPhotoFile` callbacks and must not instantiate `ProgressPhotoStorage` or call `File.delete()` for progress-photo lifecycle work.
- 2026-07-05: Room schema version 15 adds lookup indexes for cleanup/link-reference columns: `counter_projects.linkedPatternId` and `yarn_cards.linkedProjectId`. `MIGRATION_14_15` creates `index_counter_projects_linkedPatternId` and `index_yarn_cards_linkedProjectId`, and the current schema export is `app/schemas/com.finnvek.knittools.data.local.KnitToolsDatabase/15.json`.
- 2026-06-15: GitHub Actions `pull_request` -workflowt eivät enää kirjoita `app/google-services.json`-tiedostoa eivätkä pysähdy puuttuvaan `KNITTOOLS_GOOGLE_SERVICES_JSON_BASE64` -secretiin. Build & Test ja CodeQL ajavat `assembleDebug`in suoraan; Gradlen `writeGoogleServicesJsonFromEnv` ja `writeDebugGoogleServicesJson` omistavat debug Firebase -konfigin materialisoinnin, jolloin puhdas PR-checkout käyttää ignored `app/src/debug/google-services.json` -placeholderia ilman release-configin heikentämistä.
- 2026-06-14: Android Firebase release -artifactit säilyttävät `app/google-services.json` -portin, mutta debug-artifactit eivät enää pysähdy tyhjässä checkoutissa. `app/build.gradle.kts` materialisoi oikean ignored `app/google-services.json` -tiedoston `KNITTOOLS_GOOGLE_SERVICES_JSON_BASE64` -ympäristömuuttujasta, ja jos oikeaa configia ei ole, `assembleDebug`/`installDebug`/`processDebugGoogleServices` saavat ignored `app/src/debug/google-services.json` -placeholderin. `assembleRelease` ja `bundleRelease` epäonnistuvat edelleen ilman oikeaa Firebase-configia; `:app:lint` ja `sonar` eivät saa riippua tästä portista.
- 2026-06-12: Ravelry Firebase -vaihe 9 kovensi release-surface-rajan. `tools/release-surface.ps1` sallii nyt vain Firebase Auth/Functions/Google Services -pinnan Ravelry-backendia varten, vaatii `app/google-services.json`in pysymään ignored/untracked-polussa, kieltää Firebase AI/ML Kit/Gemini/voice-riippuvuudet, pitää release Sentryn ja FileProvider-juuret ennallaan rajattuina ja skannaa paikallisista/env-arvoista tunnetut Ravelry-secretit lähteistä, resursseista, generated-vakioista, Gradlesta, manifesteista, testeistä, APK:sta ja AAB:sta tulostamatta secret-arvoa. `tools/release-surface-test.ps1` kattaa baseline-fixturen, tracked google-services -mutaation, ML Kit -mutaation ja known-secret-vuodon. Semgrep/DeepSec käsittelevät vanhat Android Ravelry BuildConfig/Gradle secret -pinnat removed-risk-regressioina.
- 2026-06-12: Ravelry UI/Saved Patterns -vaihe 8 viimeisteli Androidin saved-pattern UX:n. `RavelryImportConfirmationSheet` käsittelee sekä hakutuloksen että Android Sharesheetin validoidun Ravelry pattern URL:n import-vahvistuksen, duplicate-polku avaa olemassa olevan `SavedPatternDetail`-rivin, ja connected-tilan Browse Ravelry avaa Ravelryn pattern searchin Custom Tabsilla share päällä. `PatternPickerSheet` listaa kaikki saved patternit ja käyttää `CounterViewModel.attachSavedPattern` -> `CounterRepository.attachSavedPattern` -polkua; SAF PDF -liite pysyy erillisenä. Counterin pattern-kortti avaa PDF-viewerin vain `patternUri`n kanssa ja metadata-only `SavedPatternDetail` -reitin `linkedPatternId`-linkeille. Uudet Ravelry/Saved Patterns -copyt on lokalisoitu kaikkiin nykyisiin `values-*`-tiedostoihin ilman source-kategoriaotsikoita kuten "Saved from Ravelry".
- 2026-06-11: Ravelry Firebase -vaihe 7 nosti Roomin schemaan 14 ja siirsi saved patterns -taulun lähdemetadataan. `SavedPatternSource` erottaa `RAVELRY`, `LOCAL_FILE` ja `OTHER`; persisted kentät ovat `source`, nullable `ravelryPatternId`, `originalUrl`, `canonicalUrl`, nullable `localPdfUri`, `isAvailableOffline`, `updatedAt` ja nullable `lastSyncedAt`. Migraatio 13->14 säilyttää saved-pattern ID:t, backfillaa vanhan `ravelryId > 0` -rivin Ravelryksi, `content://`/`file://`-URLit paikallisiksi tiedostoiksi ja muut `OTHER`-lähteeksi. `SavedPatternRepository.findDuplicateCandidate` tarkistaa duplikaatit järjestyksessä Ravelry ID, canonical URL, normalisoitu original URL ja title+designer vain erillisellä vahvistuslipulla. Seuraava suunnitelmavaihe on URL/share-import ja saved-pattern UX.
- 2026-06-11: Android Firebase -build gate säilyy build-artefakteille (`assembleDebug`, `assembleRelease`, `bundleRelease`), mutta `:app:lint` saa ajaa ilman paikallista ignored `app/google-services.json` -tiedostoa. `verifyGoogleServicesJson` on configuration cache -yhteensopiva task-luokka, joka ei kaappaa Gradle script -objekteja task actioniin.
- 2026-06-11: Ravelry Firebase -vaihe 6 siirsi Androidin Ravelry-auth-flow'n lopullisesti backend-owned malliin. `RavelryAuthManager` omistaa `RavelryAuthState`-tilan, kutsuu `RavelryBackendClient.startAuth/authStatus/disconnect`, validoi token-free `knittools://ravelry-auth-complete?state=...` callbackin ja näyttää cancelled/expired/backend-unavailable/disconnecting/connected-as-username -tilat. `MainActivity` avaa authin AndroidX Browser Auth Tabilla (`browser` 1.9.0) ja käyttää Custom Tabsia fallbackina; search/detail UI kerää `signInLaunchRequests` eikä enää luo OAuth URIa tai omista token-tilaa.
- 2026-06-11: Ravelryn vanha backenditön accepted-risk-arkkitehtuuri on superseded. Uusi tavoite on `Ravelry Firebase Backend And Saved Patterns Plan.md`: Firebase Auth anonymous UID + Cloud Functions v2 omistavat Ravelry-secretit, token exchangen, API-kutsut, auth-statuksen, disconnectin, haun ja URL-importin. Vaihe 1 loi `config/ravelry-backend-progress.md`-auditin; nykyinen Android `BuildConfig`/`EncryptedSharedPreferences`/Basic Auth -polku on vain väliaikainen legacy-yhteensopivuus, eikä toiminnallista koodia muutettu.
- 2026-06-11: Ravelry Firebase -vaihe 2 lisäsi backend-skeletonin: root `firebase.json`, `firestore.rules` ja `functions/` TypeScript-projekti. Functions runtime on `nodejs22`, alue `europe-west1`, Secret Manager -sidokset ovat `RAVELRY_CLIENT_ID` ja `RAVELRY_CLIENT_SECRET`, ja Firestore rules kieltää client read/write -pääsyn `ravelryOAuthStates/{state}`- ja `ravelryTokens/{uid}`-kokoelmiin. Suunnitelman `firebase-admin@14.0.0` poikkesi npm peer dependencyistä, joten skeleton käyttää uusinta `firebase-functions@7.2.5`:n kanssa yhteensopivaa `firebase-admin@13.10.0`:aa ilman `--legacy-peer-deps`-pakotusta.
- 2026-06-11: Ravelry Firebase -vaihe 3 toteutti backend-owned OAuth2-flow'n `functions/src/ravelry/`-moduuleihin: `ravelryStartAuth`, `ravelryCallback`, `ravelryAuthStatus`, `ravelryDisconnect` ja `ravelryCurrentUser` käyttävät Firestoreen tallennettua `ravelryOAuthStates/{state}` PKCE-statea, server-side token exchangea ja `ravelryTokens/{uid}` token-storea. Android ei vielä kutsu backendia; search/import ja Android Firebase -integraatio ovat myöhempiä vaiheita.
- 2026-06-11: Ravelry Firebase -vaihe 4 toteutti backend search/import -pinnan: `ravelrySearchPatterns`, `ravelryImportPatternById` ja `ravelryImportPatternByUrl` ovat metadata-only callableja, käyttävät `ravelryTokens/{uid}` access tokenia, kutsuvat Ravelryn nykyisen Android-polun mukaisia `/patterns/search.json` ja `/patterns/{id}.json` endpointteja, sanitisoivat sallitut kentät eivätkä lataa tai palauta pattern-PDF-polkuja. Android-integraatio, Room schema 14 ja legacy Android-secretien poisto ovat edelleen myöhempiä vaiheita.
- 2026-06-11: Ravelry Firebase -vaihe 5 lisäsi Android Firebase -integraation: Gradle käyttää Firebase BoM `34.14.0`:aa, `firebase-auth`ia, `firebase-functions`ia ja Google Services -pluginia `4.4.4`; `app/google-services.json` pysyy ignored ja build/CI gate vaatii paikallisen tiedoston tai `KNITTOOLS_GOOGLE_SERVICES_JSON_BASE64`-secretistä generoidun tiedoston. Androidissa on `FirebaseAnonymousAuthGateway`, `RavelryBackendClient`/`FirebaseRavelryBackendClient` ja backend-vastausmapperit. Ravelry `BuildConfig`-credentialit, release opt-in -gate, Basic Auth fallback ja Androidin `EncryptedSharedPreferences` token-store poistettiin; Phase 6 hoitaa vielä UI-auth-tilan ja callback-flow'n lopullisen backend-siirron.
- 2026-05-25: Single-source-of-truth cleanup centralized project sort order in `domain/model/ProjectSortOrder`, extra counter types in `domain/model/ProjectCounterType`, yarn-card ID CSV parsing/formatting in `domain/model/YarnCardLinks`, and FileProvider authority/share URI creation in `data/storage/AppFileStorage`. `ProjectCounterRepository` now applies `ProjectCounterLogic` from the latest stored row inside a `DatabaseTransactionRunner` transaction, and `ProjectYarnNoteRepository.saveToMyYarn` creates the linked yarn card plus saved-note reference in one repository transaction.
- 2026-05-24: Project-only yarn notes added as Room schema version 12. `project_yarn_notes` is project-scoped with cascade cleanup, `ProjectYarnNoteRepository` owns save/delete/observe plus `saveToMyYarn`, and the counter yarn sheet now separates "Choose from My Yarn" from project-only yarn notes.
- 2026-05-24: Project workspace copy cleanup completed. Pattern camera is described as photo/PDF capture instead of scan/scanner, My Yarn copy promises yarn notes, quantities, and linked projects, `YarnLabelPhotoStorage` and `AppLanguage.promptLanguageName()` were removed, and legacy `yarn_photos` URIs remain only as internal `AppFileStorage` compatibility roots.
- 2026-05-24: Voice/microphone counter flow removed. The app no longer declares `RECORD_AUDIO`, no longer exposes `ProFeature.VOICE_COMMANDS`, and `ui/screens/counter` must not reintroduce SpeechRecognizer/TextToSpeech command handling without a new explicit product/security decision.
- 2026-05-22: Mallipohjaiset ominaisuudet poistettiin aktiivisesta sovelluspinnasta. Build ei enää käytä Firebase-, Google Services-, App Check- tai ulkoisia malliparserikytkentöjä, eikä vanha mallipaketti ole enää tuotantoraja. Säilyneet pinnat ovat paikallinen `domain/calculator/InstructionParser`, Ravelry, Play Billing, pattern PDF -renderöinti, muistiinpanot, yarn cardit ja paikalliset laskurit.
- 2026-05-18: Insightsin suorituskykypolku keskitettiin `InsightsUiState`-aggregaatiksi. `InsightsScreen` kerää vain yhden UI-tilavirran, ja suuret sessiohistorialaskennat ajetaan `@IoDispatcher`-kontekstissa ennen Compose-keräystä. Room-skeemaversio 11 lisää `sessions`-tauluun `endedAt, startedAt` sekä `projectId, endedAt, startedAt` -indeksit, ja Insights-sessiokyselyt valitaan repositoryssa erillisistä all/project + all-time/since DAO-polkuista nullable-OR-ehtojen sijaan.
- 2026-05-16: Projektikohtaiset tarkistuskomennot keskitettiin `C:\Dev\Android-check\tools\AndroidProjectChecks.psm1`-moduuliin. KnitToolsin `tools/`-wrapperit kattavat `lc`, `ac`, `dc`, `ss`, `ds`, `ms`, `os`, `ql`, `db` ja `sc`; raportit menevat edelleen ignored `reports/`-kansioon, ja `sc -Full` lisaa Android security/DeepSec -polun perus dependency/secret/Semgrep-ajon paalle.
- 2026-05-16: Puhelinasennusta varten lisattiin `tools/ad.ps1`, joka delegoi `C:\Dev\Android-check\tools\InstallDebugToDevice.ps1`-skriptiin. `ad` ajaa oletuksena `assembleDebug`, lukee `adb.exe`-polun projektin `local.properties` `sdk.dir`-arvosta ja asentaa debug APK:n ilman globaalia PATH-riippuvuutta; `ad -NoBuild` asentaa jo rakennetun APK:n.
- 2026-05-16: Tarkistuskomentoihin lisattiin `pc`, `cs`, `cr` ja `ga`. `pc` ajaa PMD CPD -duplikaattitarkistuksen KnitToolsin oletusrajalla `PMD_CPD_MINIMUM_TOKENS=100`, `cr` ajaa compose-rulesin ktlint/detekt-polkujen kautta, `ga` ajaa Google Android Security Lints -saannot Android Lintin mukana ja `cs` on varattu Compose Stability Analyzer -projekteille. KnitTools kayttaa compose-rules 0.5.8 -versiota seka Google security lint 1.0.4 -lintChecks-riippuvuutta.
- 2026-05-16: DeepSecin Ravelry `secrets-exposure` -HIGH-löydökset pidetään backendittömän arkkitehtuurin hyväksyttynä riskinä. `.deepsec/scripts/mark-accepted-risks.mjs` merkitsee vain dokumentoidut Ravelry credential -löydökset `accepted-risk`-tilaan revalidoinnin jälkeen, jotta export piilottaa ne rikkomatta Ravelry-toimintoa.

## 2026-05-08

- 2026-05-10: Widget counter -launchit kovennettiin intent-spoofingia vastaan. `MainActivity.createCounterLaunchIntent()` rekisteröi launch-id:n `data/storage/CounterLaunchTokenStore`en, `MainActivity` hyväksyy counter-extra-reitityksen vain tunnetulla id:llä, ja OAuth-callback-intentit eivät saa käynnistää counter-navigointia vaikka mukana olisi counter-extra-arvoja.
- 2026-05-15: Ravelry-autentikointi pidettiin tietoisesti backendittömänä. OAuth-valtuutus lähettää PKCE-parametrit, mutta token exchange käyttää edelleen client secretiä ja API fallback käyttää Basic Authia, koska Ravelrylle ei tule backendia. Release build vaatii `KNITTOOLS_ALLOW_EMBEDDED_RAVELRY_SECRETS=true` ja riskipäätös on dokumentoitu `config/security-decisions.md`:ssä.
- 2026-05-09: Widgetistä avattu counter käyttää nyt kertakäyttöistä `CounterLaunchRequest.requestId`-tunnistetta. `MainActivity` tallentaa käsitellyn tunnisteen `savedInstanceState`en, tyhjentää intent-extrat kuittauksen jälkeen ja hyväksyy `onNewIntent`-polun uutena käyttäjän käynnistyksenä.
- 2026-05-09: Debug-only Ravelry credentialit kuuluvat ignored `debug.credentials.properties` -tiedostoon, eivät `local.properties`-tiedostoon.
- 2026-05-09: Vanhat relocation-kopiot poistettiin: `ui/screens/pattern/PdfPageRenderer.kt`. Canonical-toteutus on `data/storage/PdfPageRenderer.kt`.
- 2026-05-12: IO-dispatcher keskitettiin Hiltin `di/DispatchersModule`-moduuliin (`@IoDispatcher`). Repositoryt ja ViewModelit injektoivat IO-dispatcherin sen sijaan, että ne käyttäisivät suoraan `Dispatchers.IO`:ta.
- 2026-05-12: Monivaiheiset Room-kirjoitukset keskitettiin repositoryihin `data/local/DatabaseTransactionRunner`in kautta. Yarn/project-linkitykset kulkevat `YarnCardRepository`n kautta: `saveCard` normalisoi tallennettavan `linkedProjectId`-arvon ja `updateLinkedProjectId` päivittää eksplisiittiset relinkit sekä `yarn_cards.linkedProjectId`-kenttään että projektien `yarnCardIds`-listoihin yhdessä. Pattern attach/detach kulkee `CounterRepository.attachPattern` / `detachPattern` -metodien kautta, jotta saved pattern -rivi, annotaatiot ja projektin pattern-kentät pysyvät atomisina.
- 2026-05-13: Pattern-PDF:t tallennetaan appin omistamina tiedostoina `pattern_pdfs/<projectId>`-hakemistoon, ja ohjekuvasta PDF:ksi -virran väliaikaiset kuvat ovat erillisessä `pattern_captures/<projectId>`-hakemistossa. `SavedPatternRepository.deleteLocalPatternFileIfUnused` on keskitetty cleanup-portti saved pattern -poiston, projektin pattern-irrotuksen ja projektin poiston jälkeen; vanhat `patterns/...` FileProvider-URI:t avataan ja poistetaan `AppFileStorage`n sisäisellä resolvauksella.
- 2026-05-13: Sovelluksen runtime-kielen lähde on AppCompat/Android per-app locale -API. `PreferencesManager` käyttää DataStoren `app_language`-arvoa vain tallennus- ja migraatiopeilinä, migroi vanhan valinnan kerran Android 13+ -järjestelmälle ja synkkaa järjestelmäasetuksista tehdyt muutokset takaisin DataStoreen.
- 2026-05-14: Widget-rivilaskurin kirjoitus kulkee nyt `CounterRepository.applyWidgetCountChange`-metodin kautta. Metodi lukee nykyisen projektirivin ja kirjoittaa countin, historian sekä `currentStitch`-nollauksen saman `DatabaseTransactionRunner`-transaktion sisällä, jotta nopeat widget-tapit eivät tallenna vanhentuneita history-arvoja.
- 2026-05-14: Session tracking käyttää Room-skeemaversiosta 10 alkaen `sessions.durationSeconds`- ja `sessions.rowsWorked`-kenttiä. Counter katkaisee aktiivisen session historian avaamisen ja appin taustalle siirtymisen yhteydessä, session history tukee yksittäisen session poistoa, ja Insights laskee rows/hour-arvon sekunneista sekä jakaa keskiyön ylittävät sessiot paikallisen päivämäärän mukaan.
- 2026-05-14: Legacy secondary counterin lähde on `counter_projects.secondaryCount`. `project_counters` on vain nimetyille lisä-, repeating-, shaping- ja repeat-section-laskureille, migraatiot eivät kopioi `secondaryCount`-arvoa erilliseksi `project_counters`-riviksi, ja vanhat generoidut `Pattern repeat` -backfill-kopiot ohitetaan counter-UI:n rajalla dataa poistamatta.
- 2026-05-14: Projektimuistiinpanojen korvaavat kirjoitukset kulkevat `CounterRepository.saveProjectNotes`-metodin kautta. Notes-editori seuraa projektivirtaa metatiedoille ja käyttää viimeksi havaittuja muistiinpanoja merge-pohjana, jotta rinnakkaiset editorikirjoitukset eivät ylikirjoita toisiaan.
- 2026-05-15: Yarn card detail -reitti seuraa avattua korttia `YarnCardRepository.observeCard`-virran kautta. Jos korttirivi poistuu detail-näkymän ollessa auki, route tyhjentää lomaketilan ja poistuu näkymästä; detailin status-, määrä- ja projektilinkkimuutokset eivät enää päivitä UI-tilaa pelkästään optimistisesti, vaan tila päivittyy repository-kirjoituksen jälkeisestä havainnoidusta rivistä.
- 2026-05-09: Counterin pattern-kuvake näytetään vain, kun projektilla on oikea `patternUri`; pelkkä Ravelry/SavedPattern-metadata `linkedPatternId` ei enää avaa PDF-vieweriä ilman liitettyä PDF:ää.
- Vanha `yarn_photos`-polku on vain `AppFileStorage`n sisäinen yhteensopivuusjuuri vanhojen URIen lukua/siivousta varten; älä palauta `YarnLabelPhotoStorage`-luontivirtaa tai yarn label -scan-copya.
- `ProFeature`-gateja käytetään featurekohtaisten enum-arvojen kautta; Insights tarkistaa `ProFeature.INSIGHTS_CHARTS`in eikä yleistä `isPro()`-tilaa.
- Room-entityjen irrotus on aloitettu lisäämällä Room-vapaat domain-mallit `domain/model`-pakettiin sekä kaksisuuntaiset entity/domain-mapperit `data/local/EntityMappers.kt`:hen.
- `CounterRepository`, `SavedPatternRepository`, `ProgressPhotoRepository`, `YarnCardRepository`, `PatternAnnotationRepository`, `ProjectCounterRepository` ja `ReminderRepository` paljastavat nyt public API:ssaan domain-mallit ja muuntavat entityiksi vain DAO-rajalla. `CounterProject` ja `KnitSession` ovat pääprojektien ja sessioiden UI-/repository-rajapinnan mallit.
- `domain/calculator` ei enää käytä `ProjectCounterEntity`- tai `RowReminderEntity`-tyyppejä; counterin apulaskurit ja rivimuistutukset kulkevat `ProjectCounter`- ja `RowReminder`-domain-malleilla.
- `ui/` ei importtaa Room entityjä tai `data/local`-pakettia entity-irrotuksen vaiheessa 5; pääprojektit ja sessiot kulkevat domain-malleina ViewModel- ja composable-kerroksissa.
- 2026-07-28: Projektikohtainen DeepSec päivitettiin täsmälleen versioon `2.2.9` sekä `package.json`issa että pnpm-lukituksessa. `tsc --noEmit` ja matcher-testit 6/6 läpäisivät; ulkoista AI-analyysiä ei ajettu.
- 2026-07-28: `tools/sc.ps1` on security-checkin ainoa source of truth. Vanhat bash- ja global-entrypointit delegoivat siihen fail-closed eivätkä enää sisällä omaa Semgrep-/dependency-check-toteutusta.
