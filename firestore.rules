rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /ravelryOAuthStates/{state} {
      allow read, write: if false;
    }

    match /ravelryTokens/{uid} {
      allow read, write: if false;
    }

    match /{document=**} {
      allow read, write: if false;
    }
  }
}
